# 기술 발표용 PPT 자동 제작 에이전트 — 설계 배경 및 결정 기록

> 이 문서는 실행 규칙을 정의하지 않는다. 실행 규칙(워크플로우 단계, 산출물 경로, Agent/Skill 구성 등)의 Source of Truth는 [CLAUDE.md](CLAUDE.md)와 각 Agent/Skill 자체 문서(`AGENT.md`/`SKILL.md`)다. 이 문서는 그 규칙들이 왜 이런 형태가 되었는지 — 설계 배경, 핵심 설계 결정, 트레이드오프 — 만을 기록한다. 실행 규칙과 이 문서 내용이 어긋나면 실행 규칙이 우선한다.

---

## 1. 배경과 목적

사용자가 보유한 원본 자료(문서, 데이터, 이미지 등)를 매번 수작업으로 슬라이드로 옮기고 디자인을 맞추는 데 시간이 많이 든다. 특히 고객사·외부 청중을 대상으로 하는 발표는 신뢰도와 브랜딩이 중요해 디자인 일관성 유지 부담이 크다.

이를 해결하기 위해 사용자가 제공한 자료를 분석해 슬라이드 구성과 핵심 메시지를 설계하고, 일관된 디자인 규칙을 적용해 PPT를 제작하는 에이전트를 만든다. 빠른 확인·수정을 위해 웹 기반 PPT로 초안을 만들고, 확정 후 `.pptx`로 출력한다.

## 2. 범위 중 설계적으로 의미 있는 내용

- **제외 대상은 의도적으로 명확히 그었다**: 원본 자료 자체의 사실 검증(팩트체크), 발표 리허설/스크립트 작성, 자료 원본 편집은 이 에이전트의 책임이 아니다. 에이전트가 다루는 것은 "이미 사실로 주어진 자료를 어떻게 슬라이드로 구성·표현할 것인가"이며, 자료의 사실 여부 판단이나 원본 자체의 수정은 사용자 영역으로 남긴다. 이 경계가 없으면 수치·주장의 임의 생성/추정 금지 원칙(§10)과 충돌한다.
- 다국어(번역 포함) 지원은 범위에 포함하되, 원본이 한국어라도 요청 시 생성 가능하도록 설계했다 — 청중이 고객사/외부인 경우 언어가 발표 직전에 바뀌는 경우가 실무상 흔하기 때문이다.

## 3. LLM 판단 영역과 코드 처리 영역을 구분한 이유

각 단계를 설계할 때 "이 판단은 LLM이 해야 하는가, 결정론적 코드로 처리해도 되는가"를 먼저 나눴다. 기준은 다음과 같다.

- **LLM 판단**: 핵심 메시지·데이터 중요도 판단, 슬라이드 분할과 스토리라인 순서, 디자인 가변 규칙의 맥락적 해석, 피드백 의도 해석처럼 "정답이 하나가 아니고 맥락에 따라 달라지는" 판단.
- **코드 처리**: 파일 파싱, 구성안 직렬화, HTML/CSS 렌더링, pptx 변환·무결성 검증처럼 "입력이 같으면 결과도 같아야 하는" 결정론적 작업.

이 구분을 명시적으로 둔 이유는, LLM 판단이 필요 없는 영역(특히 pptx 변환)까지 매번 LLM에게 맡기면 재현성이 떨어지고 실패 원인 추적이 어려워지기 때문이다. 반대로 코드로 고정할 수 없는 영역(슬라이드 구성, 디자인 해석)을 규칙만으로 처리하려 하면 자료마다 달라지는 맥락을 반영하지 못한다. 이 원칙은 서브에이전트 분리(§4)와 pptx 변환을 별도 결정론적 단계로 둔 이유(§7)의 근거이기도 하다.

## 4. 서브에이전트를 분리한 이유

`content-designer`와 `pptx-converter` 2개로 분리했다. 근거:

- 자료분석·슬라이드설계·웹PPT생성(디자인 규칙 해석)은 서로 맥락을 공유해야 하는 연속적 판단이라 하나의 컨텍스트로 묶었다.
- pptx 변환은 완전히 다른 도구 지식(렌더링·파일 포맷 재구성)이 필요하고, §3의 기준에 따르면 디자인 판단이 거의 없는 결정론적 작업에 가깝다. 별도 서브에이전트로 분리하면 이 단계에서 불필요한 디자인 판단 컨텍스트를 로드하지 않아도 된다.
- 두 Human Review 체크포인트([4], [6])가 마침 이 분리 경계와 자연스럽게 일치해, 서브에이전트 전환 지점이 사용자에게도 명확하다.

서브에이전트 간 직접 호출을 금지하고 모든 조율을 메인이 담당하도록 한 것도 같은 맥락이다 — 컨텍스트가 분리된 두 에이전트가 서로를 직접 부르면 어느 쪽 판단 기준을 따라야 하는지 불명확해지므로, 조율 책임을 항상 메인 하나로 고정했다.

## 5. 고정 규칙 / 가변 규칙을 분리한 이유

디자인 규칙을 **고정 규칙**(회사 로고, 브랜드 요소, 지정된 표지 등 반드시 유지해야 하는 요소)과 **가변 규칙**(레이아웃, 차트·표 스타일, 정보 시각화, 강조 방식 등 목적에 맞게 구성 가능한 요소)으로 나눴다.

회사 PPT 디자인 전체가 처음부터 확정돼 있지 않은 상태에서 시작했기 때문에, 모든 규칙을 하나의 고정된 세트로 취급하면 실제로 바뀔 수 있는 부분(가변 규칙)까지 매번 예외 처리해야 하는 문제가 생긴다. 반대로 모든 규칙을 자유롭게 바꿀 수 있다고 하면 브랜드 신뢰도를 해치는 변형(로고 훼손, 표지 임의 변경 등)까지 허용하게 된다. 이 이원화 덕분에 가변 규칙은 레퍼런스와 목적에 따라 누적·개정 가능한 자산으로 관리하면서도, 고정 규칙은 사용자의 명시적 개입 없이는 절대 바뀌지 않는다는 보장을 유지할 수 있다.

## 6. 레퍼런스 자료를 복제 기준이 아닌 참고 자료로 둔 이유

레퍼런스 자료(사용자가 제공하는 PPT/이미지)는 디자인을 그대로 복제하기 위한 기준이 아니라, 디자인 방향과 우수한 레이아웃·시각화 패턴을 참고하기 위한 자료로 취급하도록 설계했다.

레퍼런스를 복제 기준으로 삼으면 두 가지 문제가 생긴다: (1) 레퍼런스마다 스타일이 다를 때 어느 하나를 그대로 베낄 근거가 없고, (2) 레퍼런스의 디자인이 이번 발표의 청중·목적에 맞지 않을 수 있다. 그래서 여러 레퍼런스가 제공되면 각각의 장점을 분석해 이번 목적에 맞는 가변 규칙을 도출하는 방식을 택했다. 레퍼런스들이 서로 다른 스타일(예: 미니멀 vs 데이터 중심)을 보여 가변 규칙 도출이 충돌하는 경우, 임의로 하나를 선택하지 않고 사용자에게 선택지를 제시해 확인하도록 한 것도 같은 이유다 — 스타일 선택은 결국 브랜드 판단이라 LLM이 임의로 결정할 성격이 아니다.

## 7. 웹PPT → pptx 구조를 선택한 이유

1차 산출물을 웹 기반 PPT(HTML/CSS)로 만들고, Human Review를 거쳐 확정된 뒤에만 `.pptx`로 변환하는 2단계 구조를 택했다.

- 웹PPT는 브라우저에서 즉시 렌더링되어 반복 수정 주기가 빠르다 — pptx로 직접 만들고 매번 파워포인트를 열어 확인하는 것보다 피드백 반영 속도가 빠르다.
- pptx 변환은 §3 기준상 판단이 거의 없는 결정론적 작업이므로, 판단이 필요한 반복 수정 단계(웹PPT)와 판단이 필요 없는 최종 변환 단계(pptx)를 분리하면 변환 단계에서 디자인이 흔들릴 위험이 없다.
- pptx 변환 완료 후에도 재수정 요청이 있으면 pptx를 직접 편집하지 않고 반드시 웹PPT로 돌아가 수정한 뒤 재변환하도록 한 것도 같은 이유다 — pptx를 직접 고치면 그 변경 사항이 웹PPT 쪽 소스와 어긋나 다음 재변환 때 유실된다.

## 8. 폴더 스냅샷 기반 버전 관리를 선택한 이유

웹PPT 수정 이력은 git이 아닌 단순 폴더 복사(`web_ppt/v1/`, `v2/` ...) 방식으로 관리한다.

발표 자료는 개발자가 아닌 사용자가 브라우저로 직접 열어보며 검토하는 대상이라, git 이력을 통해 버전을 추적하는 방식은 사용자에게 실질적인 이점이 없고 오히려 진입장벽이 된다. 폴더 스냅샷은 사용자가 파일 탐색기에서 바로 이전 버전을 열어보거나, 특정 버전 폴더를 복사해 롤백할 수 있어 검토·복구 과정이 더 직관적이다. 또한 프로젝트(발표 자료)마다 산출물 폴더를 분리해, 과거 발표를 나중에 다시 열어 이어서 수정할 수 있게 한 것도 같은 설계 의도다.

## 9. Human Review를 둔 이유

구성 검토([4])와 웹PPT 검토([6]), 두 차례의 Human Review를 필수 관문으로 뒀다.

- [4]는 슬라이드로 렌더링되기 전, 구성(스토리라인·메시지 배치) 단계에서 오류를 잡기 위한 지점이다 — 이미 렌더링된 뒤 구성을 바꾸면 되돌려야 할 작업이 훨씬 많다.
- [6]은 실제 시각적 결과물(레이아웃, 톤, 브랜드 적합성)을 사람이 아니면 최종 판단할 수 없는 영역이라 뒀다. 특히 청중이 고객사/외부인 경우 신뢰도·브랜딩에 대한 최종 승인은 LLM의 자기 검증만으로 대체할 수 없다고 판단했다.
- 두 지점 모두 "승인 전까지 다음 단계로 진행하지 않는다"는 규칙을 명시적으로 둔 이유는, 자동화 파이프라인에서 검토 없이 다음 단계로 흘러가면 오류가 여러 단계에 걸쳐 누적된 뒤에야 발견되기 때문이다.

## 10. 수치 임의 생성 금지의 설계 이유

원본 자료의 수치, 실험 결과, 기술적 의미는 임의로 생성·변경·추정하지 않는다. 확인이 어려운 핵심 수치(발표 주장의 근거)는 즉시 작업을 중단하고 사용자 확인을 기다리며, 부가적인 수치는 `[확인필요]` 표시만 남기고 계속 진행한다. 차트의 원본 수치를 추출할 수 없을 때도 눈대중으로 추정한 새 차트를 만들지 않고, 원본 차트를 그대로 쓰거나 사용자에게 원본 데이터를 요청한다.

이 원칙을 둔 이유는, 이 에이전트가 다루는 발표 자료가 고객사·외부 청중을 대상으로 하는 신뢰도 기반 커뮤니케이션이기 때문이다. LLM이 그럴듯하지만 부정확한 수치를 채워 넣으면 발표 전체의 신뢰도가 무너지고, 문제가 발표 당일에야 발견될 위험이 크다. "핵심 수치는 중단, 부가 수치는 표시 후 계속"으로 나눈 것은, 모든 불확실성에 대해 작업을 멈추면 진행이 사실상 불가능해지지만, 발표 주장의 근거가 되는 수치만큼은 예외 없이 사람 확인을 거치게 하기 위해서다.

---

## 11. 미결 사항

### 11.1 설계 미결 (아직 결정되지 않음 — 임의로 결정하지 않는다)

- **PII/민감정보 마스킹 여부**: 원본 자료 로컬 처리 원칙(§5, CLAUDE.md 참조)과는 별개로, 원본 자료에 포함될 수 있는 개인정보·민감정보를 슬라이드 반영 전에 마스킹하는 별도 단계를 워크플로우에 추가할지 여부가 아직 결정되지 않았다.
- **대용량 표에서 핵심 행·열 선별 기준**: 상세 데이터 표(수십 행 이상)를 부록 슬라이드로 분리해야 한다는 임계값 원칙은 있으나(material-analysis 스킬), 실제로 "어떤 행·열을 핵심으로 선별할지"에 대한 구체적 로직/기준은 아직 정의되지 않았다.

### 11.2 구현 격차 (설계는 결정됐으나 구현이 아직 못 미친 부분)

- **pptx 네이티브 차트 재구성**: 웹PPT 차트는 Chart.js로 렌더링하고, `data-chart-mode="native"`로 지정된 차트는 pptx 변환 시 python-pptx 네이티브 차트 객체로 재구성한다는 설계는 확정돼 있다(web-ppt-generator 스킬). 그러나 pptx-exporter의 실제 구현은 아직 이 네이티브 재구성 경로를 완성하지 못해, 현재는 모든 차트가 이미지로 래스터화된다. 이는 설계 선택의 문제가 아니라 pptx-exporter 쪽 구현 진행 상황의 문제이며, 이번 문서 경량화 작업에서 구현하지 않는다.
