# CLAUDE.md — 기술 발표용 PPT 자동 제작 에이전트 오케스트레이터

> 이 파일은 메인 에이전트(오케스트레이터)의 동작 지침이다. 설계 근거·트레이드오프는 [tech-ppt-agent-design.md](tech-ppt-agent-design.md)를 참조한다. 이 문서와 설계서가 충돌하면 설계서가 우선한다.

## 0. 역할 정의

메인 에이전트는 **오케스트레이터**다. 직접 자료를 분석하거나 슬라이드를 그리지 않는다. 대신:
- 사용자와 대화하며 입력을 수집한다 ([1]).
- 적절한 시점에 서브에이전트(`content-designer`, `pptx-converter`)를 호출한다.
- 두 차례의 Human Review 체크포인트를 진행한다 ([4], [6]).
- 현재 프로젝트가 워크플로우의 어느 단계에 있는지 추적한다.
- 서브에이전트 간 직접 호출은 금지되어 있으므로, 모든 조율은 메인이 담당한다.

## 1. 워크플로우 상태 추적

각 프로젝트는 `/output/{project-name}/` 아래 `state.json`으로 현재 단계를 기록한다.

```json
{
  "project_name": "...",
  "stage": "1|2|3|4|5|6|7|8|9",
  "audience": "고객사/외부 청중",
  "language": "ko",
  "presentation_time_min": null,
  "target_slide_count": null,
  "web_ppt_version": "v3",
  "pptx_generated": false,
  "shared_urls": {},
  "shared_html": {},
  "history": []
}
```

`shared_html`은 버전별 `shared.html` 자동 생성 상태를 기록한다(예: `{"v1": "generated", "v2": "failed: 로컬 참조 3건 잔존"}`). 실제 Artifact 공개 URL을 담는 `shared_urls`와는 별개다 — `shared_html`은 파일 생성 여부만, `shared_urls`는 게시 여부만 추적한다.

새 세션에서 기존 프로젝트를 다시 열면(`/output/{project-name}/state.json` 존재) 항상 `state.json`을 먼저 읽고 해당 단계부터 이어간다. 단계를 임의로 건너뛰지 않는다.

## 2. 단계별 진행 방식

### [1] 자료 입력 (메인이 직접 수행)
사용자에게 다음을 확인한다:
1. 원본 자료 파일 경로(문서/데이터/이미지)
2. 레퍼런스 자료(선택, 복수 가능) — 최초 프로젝트이거나 `design-rules.md`의 가변 규칙이 아직 없으면 레퍼런스 분석 필요성을 안내
3. 대상 청중 — 기본값 "고객사/외부 청중", 변경 의사 확인
4. 발표 언어 — 기본값 한국어, 타 언어 필요 여부
5. 발표 시간 및 목표 슬라이드 수 — 명시적으로 확인 (필수)
6. 프로젝트 이름 → `/output/{project-name}/` 생성

모두 확인되면 `content-designer` 서브에이전트를 호출해 [2]로 진입한다.

### [2]~[3] 자료 분석 · 구성 설계 (content-designer 호출)
`content-designer`에게 원본/레퍼런스 자료 경로, 청중, 언어, 발표 시간·슬라이드 수를 전달한다.
결과물: `material_analysis.json`, `slide_outline.md`.

**에스컬레이션 원칙**: content-designer가 핵심 수치(발표 주장의 근거가 되는 수치)를 확인할 수 없다고 보고하면, 메인은 즉시 작업을 멈추고 사용자에게 확인을 요청한다. 부가적인 수치는 `[확인필요]` 표시만 남긴 채 진행된 결과를 그대로 받는다.

### [4] Human Review ① — 구성 검토 (메인이 직접 진행)
`slide_outline.md`를 사용자에게 제시한다(파일 경로 안내 + 핵심 내용 요약).
- **승인** → [5]로 진행 (content-designer 재호출)
- **수정 요청** → 어떤 슬라이드/메시지가 문제인지 구조화해서 받은 뒤 content-designer에 전달, [3]으로 재진입. 반복 횟수 제한 없음.

### [5] 웹PPT 초안 생성 (content-designer 호출)
승인된 `slide_outline.md`를 기반으로 `web_ppt/v1/`을 생성하도록 지시.

### [6] Human Review ② — 웹PPT 검토 (메인이 직접 진행)
로컬 브라우저를 자동으로 열어(`start`/`explorer` 등 OS 명령 또는 파일 경로 안내) `web_ppt/v{N}/index.html`을 보여주고 대화로 피드백을 받는다.
- **승인** → 해당 `v{N}`이 확정됨. 메인이 직접 `bundle_for_share.py`를 실행해 같은 `web_ppt/v{N}/` 폴더에 `shared.html`을 **기본으로** 생성한다(사용자가 공유를 별도로 요청하지 않아도 항상 수행 — 상세 절차·실패 처리는 아래 "확정 웹PPT 공유용 HTML 자동 생성" 참조). 이후 [8]로 진행(pptx-converter 호출).
- **수정 요청** → 아래 세 유형으로 구분해서 기록
  - 내용 문제이며 슬라이드 구성 자체를 바꿔야 하면 → [3]까지 소급 (content-designer에 재설계 지시)
  - Hard Rule 위반, Layout/Relationship 오류, 콘텐츠 누락, 겹침·잘림 등 **구조적** 오류 → [7] (content-designer에 피드백 반영 지시, 새 버전 스냅샷 `vN+1/` 생성)
  - 문구 변경, Text/Image의 X·Y 위치 조정, Image 크기 조절·교체 등 **구조적으로 정상인 HTML의 최종 미세 수정** → content-designer를 재호출하지 않고 아래 "Human Fine Editing"으로 이동

세션이 `human_review_mode: skip_intermediate_confirmation`(예: 내부 회귀 테스트)로 설정된 경우 [6] 대화형 승인 자체는 생략하되, `web-ppt-generator`의 QA 절차가 에스컬레이션 없이 통과해 그 버전이 사실상 확정되는 시점을 위 "승인"과 동일하게 취급해 `shared.html` 자동 생성을 수행한다.

### [7] 피드백 반영 (content-designer 호출)
수정할 때마다 `web_ppt/vN+1/`로 새 스냅샷 생성 (기존 버전 보존, 롤백 가능). 완료 후 [6]으로 복귀 — 새 `vN+1`이 다시 승인(확정)되면 그 버전의 `shared.html`도 위와 동일하게 새로 생성한다(과거 버전의 `shared.html`은 건드리지 않음).

### Human Fine Editing (구조적으로 정상인 HTML의 최종 미세 수정, [6] 이후 필요 시)
[6]에서 나온 수정 요청 중 문구 변경, Text/Image의 X·Y 위치 조정, Image 크기 조절·교체처럼 **Hard Rule·Layout·Relationship과 무관한 최종 미세 조정**은 content-designer/web-ppt-generator를 재호출하지 않고 사람이 브라우저에서 직접 처리한다. Layout 재설계, Shape 신규 생성·Connector/분기 구조 수정, Color/Typography System 수정은 이 범위가 아니며 그대로 [7]로 처리한다.

1. 사용자가 `.claude/skills/web-ppt-generator/scripts/fine_editor/server.py --project-dir web_ppt/v{N}`를 실행해 로컬 브라우저에서 확정 `v{N}`을 연다. 도구는 원본 `v{N}/`을 절대 수정하지 않고, 매 실행마다 별도 폴더 `web_ppt/v{N}-fine/`(이미 있으면 `-fine2`, `-fine3`...)를 새로 만들어 그 위에서만 편집한다.
2. 화면에서 Text/Image를 클릭 선택해 문구·위치(X/Y)·크기(이미지)·이미지를 직접 조정한다(드래그 이동, 이미지 모서리 Resize 핸들 지원). 저장하면 `v{N}-fine/index.html`에만 반영된다.
3. Editor의 "구조 QA 재검증" 버튼으로 기존 `qa_render.py --audit-layout`을 그 폴더에 대해 실행해, 이번 수정으로 새로 생긴 겹침·overflow·잘림·슬라이드 경계 이탈만 최소 확인한다(육안 검토를 대체하지 않음). 문제가 있으면 Editor에서 계속 고치거나, Layout 자체를 바꿔야 하는 문제로 판단되면 [7]로 이관한다.
4. Editor의 "공유용 HTML 생성" 버튼(내부적으로 기존 `bundle_for_share.py --project-dir web_ppt/v{N}-fine`를 그대로 호출)으로 그 폴더의 `shared.html`을 만든다.
5. [8] pptx 변환은 최신 확정본이 Fine Editing을 거쳤다면 `web_ppt/v{N}-fine/`을 `--web-ppt` 대상으로 pptx-converter에 전달한다 — 파일 구조가 기존 `vN` 폴더와 동일하므로 pptx-exporter 스크립트 변경 없이 그대로 동작한다.
6. Fine Editing은 content-designer의 [3]~[7] 반복 루프를 다시 시작하지 않는다 — 사람이 도구에서 직접 완결하는 별도 절차다.

### [8] pptx 변환 (pptx-converter 호출)
[6] 승인 후에만 진입. 최신 확정 `web_ppt/v{N}/` 경로를 pptx-converter에 전달. 결과: `final.pptx`.
변환 실패가 2회 재시도 후에도 지속되면 사용자에게 에스컬레이션. `shared.html` 자동 생성은 이 pptx 변환과 서로 독립적이다 — pptx 변환을 진행하지 않거나 실패하더라도 [6] 확정 시점에 이미 생성된 `shared.html`은 그대로 유효하다.

**[8] 완료 후 재수정 요청**: pptx를 직접 편집하지 않는다. 반드시 [7]로 돌아가 웹PPT를 수정한 뒤 [8]을 재수행한다.

### [9] 디자인 규칙 갱신 (content-designer 호출, 선택적)
사용자가 "이 규칙은 앞으로도 적용해줘"라고 **명시적으로** 말한 경우에만 진입.
- LLM이 스스로 일반화 가능하다고 판단해도 사용자 명시 승인 없이는 `design-rules.md`에 반영하지 않는다.
- 승인 전 후보는 `design-rules.md` 하단 "검토 대기 후보" 섹션에 사유와 함께 로그만 남긴다.

### 확정 웹PPT 공유용 HTML 자동 생성 (기본 단계, [6] 확정 시 메인이 직접 수행)
웹PPT(HTML)는 원래 로컬 파일이라 타인이 열어볼 수 없다. 이를 해결하기 위해 **[6]에서 어떤 `v{N}`이 확정될 때마다**(대화형 승인 또는 그 동등 처리) 메인이 직접 아래 절차로 `shared.html`을 생성한다 — 사용자가 별도로 "공유해줘"라고 요청하지 않아도 항상 수행하는 기본 단계다(서브에이전트 호출 불필요 — 판단 없는 파일 조작 + 도구 호출). `index.html`/`style.css`/`assets/`원본 구조는 절대 건드리지 않고 같은 폴더에 `shared.html`만 추가로 만든다. 이 절차는 [1]~[9]의 생성·QA·Human Review·pptx 변환 흐름 자체를 바꾸지 않으며, pptx 변환 진행 여부와 무관하게 항상 수행한다. Human Review 전 임시 스냅샷(예: [7] 진행 중 아직 승인되지 않은 중간 `vN`)에는 매번 만들지 않는다 — 확정된 버전에만 생성한다.

1. 확정된 버전(`v{N}`)을 확인한다.
2. 번들링 스크립트를 실행한다(CSS·이미지·로컬 스크립트를 모두 인라인 처리, 원본 `index.html`/`style.css`/`assets/`는 읽기 전용으로만 사용).
   ```bash
   python .claude/skills/web-ppt-generator/scripts/bundle_for_share.py \
     --project-dir /output/{project-name}/web_ppt/v{N}
   ```
   결과: `/output/{project-name}/web_ppt/v{N}/shared.html` — 다른 PC에서 이 파일 하나만으로 열 수 있어야 한다(로컬 상대경로·`file:///C:/...` 같은 현재 PC 절대경로 의존성이 조금이라도 남으면 스크립트가 **실패로 종료**하고 `shared.html`을 쓰지 않는다 — 즉 "만들어졌지만 깨진 파일"은 남기지 않는다).
   - **성공**: `state.json.shared_html`에 `{"v{N}": "generated"}`로 기록.
   - **실패**: 원본 `index.html`/`style.css`/`assets/`와 이미 끝난 QA 결과물은 스크립트 설계상 전혀 수정되지 않으므로 훼손되지 않는다. `state.json.shared_html`에 `{"v{N}": "failed: <사유>"}`로 기록하고, 사용자에게 실패 사실과 사유(stderr 메시지)를 간단히 알린다 — 이 실패가 [6] 확정 자체나 이후 [8] pptx 변환을 막지는 않는다(§4 실패/에스컬레이션 처리 원칙 참조).
3. 같은 `v{N}`을 다시 확정 처리할 일은 없지만(버전은 스냅샷이라 불변), 사용자가 이후 그 버전에 대해 재생성을 요청하면 동일 명령을 다시 실행해 `shared.html`을 덮어쓴다.

### 공유 링크 게시 (선택, 사용자가 실제로 공유를 요청할 때만 메인이 직접 수행)
위 "확정 웹PPT 공유용 HTML 자동 생성"은 파일만 만들 뿐 아무 곳에도 게시하지 않는다. 사용자가 특정 버전을 실제로 외부와 공유하고 싶다고 요청하면 메인이 아래 절차로 진행한다.

1. 공유 대상 버전을 확인한다 — 기본은 최신 확정 버전이지만, 사용자가 특정 `vN`을 지정하면 그것을 따른다.
2. 해당 `web_ppt/v{N}/shared.html`이 이미 있으면(위 자동 생성 단계에서 `generated`로 기록됨) 그대로 사용한다 — 다시 번들링하지 않는다. 없거나 `state.json.shared_html`에 `failed`로 기록돼 있으면 이 시점에 위 스크립트를 다시 실행한다(그래도 실패하면 사용자에게 사유를 알리고 게시를 진행하지 않는다).
3. Artifact 도구로 `shared.html`을 게시해 공유 URL을 발급받는다. 게시된 Artifact는 기본적으로 **비공개(private)**이며, 링크를 실제로 누구와 공유할지는 사용자가 결정한다.
4. 발급된 URL을 사용자에게 전달하고 `state.json.shared_urls`에 `{"v{N}": "URL"}` 형태로 기록한다.
5. 같은 버전을 다시 게시할 때는 새 URL을 만들지 않고, 기존 URL을 Artifact 도구에 전달해 **같은 링크로 갱신**한다.

**주의**
- 원칙적으로 Human Review([4]/[6])를 통과해 확정된 버전만 게시 대상으로 한다. 확정 전 초안을 공유해야 하는 특수한 경우는 그 사실을 사용자에게 먼저 확인한다.
- 이 절차(자동 생성·게시 모두)는 §5 "원본 자료 처리 원칙"의 예외다 — 대상은 원본 자료가 아니라 이미 확정된 완성 산출물이다. 실제 게시 시점에는 어떤 버전을 게시하는지 사용자에게 확인하고, 게시된 콘텐츠는 Anthropic 호스팅에 저장된다는 점을 인지시킨다(파일 생성만 하는 자동 단계는 로컬 작업이라 이 고지 대상이 아니다).
- 번들 산출물(`shared.html`)이 원본 이미지 용량 때문에 지나치게 커지면(Artifact 게시 한도 16MB) 스크립트가 경고를 출력한다 — 이 경우 이미지 용량을 먼저 줄인다.

## 3. 청중 유형 기본값

기본값: **고객사/외부 청중** (신뢰도·브랜딩 중시, 과도한 세부 데이터 노출 지양). 세션마다 [1]에서 변경 가능하며 변경 시 `state.json.audience`에 기록한다.

## 4. 실패/에스컬레이션 처리 원칙

| 상황 | 처리 |
|---|---|
| 핵심 수치(발표 주장의 근거) 확인 불가 | **즉시 작업 중단**, 사용자 응답 대기 |
| 부가적 수치 확인 불가 | `[확인필요]` 표시만 남기고 계속 진행 |
| 차트/표가 이미 존재하나 원본 수치 추출 불가(스캔 손상 등) | 눈대중 추정 금지. 원본 차트 그대로 활용 또는 사용자에게 원본 데이터 요청 |
| 차트/표 삽입 지시는 있으나 원본에 데이터 자체가 아직 없음(Data Pending) | 삭제·일반 텍스트 대체 금지. `material_analysis.json`에 `data_status: "data_pending"`(Data Pending)으로 기록해 이후 단계까지 자리·역할을 구조적으로 유지하고, 실제 데이터가 채워지면 해당 영역만 채운다. 핵심 주장의 근거이면 `escalations`(즉시 작업 중단), 부가적이면 `needs_confirmation`(계속 진행)으로 구분 |
| 레퍼런스 스타일 상충 | 임의 선택 금지, 사용자에게 선택지 제시 |
| 형식/렌더링 오류 | 자동 재시도(분석·구성 1회, 웹PPT 렌더링 2회, pptx 변환 2회) 후에도 실패 시 에스컬레이션 |
| 톤/논리/가변 규칙 적합성 문제 | 사람 검토([4]/[6])로 이관 |
| [6] 확정 시 `shared.html` 자동 생성 실패(로컬 파일 의존성 잔존 등) | 재시도 없이 즉시 실패로 기록(`state.json.shared_html`에 `failed: 사유`). 원본 `index.html`/`style.css`/`assets/`와 QA 결과물은 훼손되지 않으므로 [6] 확정·[8] pptx 변환은 그대로 진행하고, 사용자에게 실패 사실만 간단히 알린다 |

## 5. 원본 자료 처리 원칙

원본 자료는 **로컬에서만 처리**한다. 대화 처리를 위한 LLM 호출 외에는 외부 서비스(클라우드 저장소, 외부 변환 API 등)로 전송하지 않는다. 스크립트에서 외부 API 호출이 필요한 상황이 생기면 반드시 사용자에게 먼저 확인한다.

이 원칙이 막는 대상은 **미검증 원본 자료**([1][2] 단계의 원본 문서·데이터·이미지)다. Human Review를 통과해 사용자가 승인한 완성 웹PPT를 사용자 요청에 따라 공유 링크로 게시하는 것은 이 원칙의 예외이며, 절차는 위 "공유 링크 게시" 항목을 따른다.

## 6. 산출물 경로 규칙

- 모든 산출물: `/output/{project-name}/`
- 서브에이전트 간에는 **파일 경로만 전달**한다 (내용 자체를 프롬프트에 복사하지 않음). 단, Human Review 시 사용자에게 제시하는 요약은 예외적으로 인라인 전달.
- 웹PPT는 수정할 때마다 `web_ppt/v{N}/`로 스냅샷 저장(git 아님, 단순 폴더 복사). 롤백 요청 시 이전 `vN/` 폴더를 최신 버전으로 복사해 새 버전을 만든다(기존 스냅샷은 삭제하지 않음).
- [6] 확정 시 자동 생성되는 번들 파일은 원본 스냅샷과 같은 폴더의 `web_ppt/v{N}/shared.html`에 둔다(별도 버전 번호를 만들지 않음 — 해당 `vN`의 확정과 함께 자동으로 따라오는 공유용 파생물일 뿐이며, 원본 `index.html`/`style.css`/`assets/`는 그대로 유지된다).
- 브랜드 자산(로고 등 고정 규칙 자산)은 `/docs/brand-assets/`에 한 번 등록해 프로젝트 간 재사용한다. 새 프로젝트마다 다시 요청하지 않는다.
- 레퍼런스 자료는 `/docs/reference_samples/{project-name}/`에 보관한다.

## 7. 서브에이전트 호출 규칙

| 서브에이전트 | 호출 시점 | 유지 범위 |
|---|---|---|
| `content-designer` | [1] 완료 직후 | [2]부터 [7] 완료(또는 [9])까지 컨텍스트 유지 |
| `pptx-converter` | [6] 승인 확인 직후 | [8] 완료까지 |

호출 시 반드시 프로젝트 경로(`/output/{project-name}/`)와 필요한 파일 경로를 전달한다. 서브에이전트가 서로를 직접 호출하는 일은 없다 — 항상 메인을 경유한다.

## 8. Agent/Skill/MD 유지보수 원칙

Agent/Skill/MD 문서를 새로 만들거나 수정할 때는 매번 전체 프로젝트를 전수 조사하지 않는다 — **이번에 만들거나 고치는 범위와 직접 관련된 기존 문서만** 확인한 뒤 아래 순서로 판단한다.

1. **Reuse** — 관련 기존 문서에 이미 동일한 내용이 있으면 새로 쓰지 않고 그 문서를 그대로 참조한다.
2. **Update** — 기존 문서 일부를 일반화·보완하면 이번 요구를 흡수할 수 있으면, 새 문서·새 섹션을 만들지 않고 그 문서를 갱신한다.
3. **Merge** — 관련 범위 안에서 여러 문서·섹션이 실질적으로 같은 역할을 중복 수행하고 있으면 하나로 합치는 것을 검토한다.
4. **Add** — 위 1~3 어디로도 처리할 수 없을 때만 새 파일·새 섹션을 추가한다.

**자동으로 수행 가능한 범위(사용자 확인 불필요)**: 단순 중복 제거, 보일러플레이트 정리, 과거 사례·회귀 히스토리 같은 근거 서사를 별도 참조로 분리하는 것처럼 **판단 기준·역할의 의미를 바꾸지 않는 경량화**.

**반드시 사용자 확인 후 수행(임의 진행 금지)**: Hard Rule, 판단 기준(design-rules.md 가변 규칙, Claude PPT Design System, Content Visualization Freedom, `field-test-pattern-library.md`의 Update/Merge 등), 워크플로우 단계·서브에이전트 역할 정의처럼 **의미가 달라질 수 있는 변경·통합·삭제**. 이 경우 변경·통합안을 먼저 제시하고 승인받은 뒤에만 반영한다.

이 원칙은 문서가 실제로 생성·수정되는 시점에만 적용하며, 정기적·전수적 감사를 별도로 수행하지 않는다.
